import 'package:flutter/material.dart';

// This class defines the primary colors used in the application.
class PrimaryColors {
  static const Color black = Color(0xFF000E0D);
  static const Color white = Color(0xFFFFFFFF);
  static const Color grey = Color(0xFFDADFED);
  static const Color hint = Color(0xFF8687A1);
  static const Color error = Color(0xFFFF3B30);
  static const Color blue = Color(0xFF74BFFF);

  // Status colors
  static const Color success = Color(0xFF66BB6A); // Green for success messages
  static const Color danger = Color(
    0xFFEF5350,
  ); // Red for delete/danger actions
}
